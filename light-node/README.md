# Usage

```sh
cargo run --release --example light-node -- --config config.json --global-config global-config.json --logger-config logger.json ---rpc-blacklist-config rpc-blacklist.json -keys keys.json
```